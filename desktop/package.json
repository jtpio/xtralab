{
  "name": "@xtralab/desktop",
  "productName": "Xtralab",
  "version": "0.2.0",
  "description": "Electron desktop shell for Xtralab.",
  "private": true,
  "license": "BSD-3-Clause",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:python": "uv build --wheel --out-dir python/wheels ..",
    "dev": "npm run build && npm run build:python && electron .",
    "start": "npm run dev",
    "package": "npm run build && npm run build:python && electron-packager . Xtralab --out=out --overwrite --app-bundle-id=io.github.jtpio.xtralab --icon=assets/jupyter --ignore='^/out($|/)|^/python/wheels/\\.gitignore$'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@electron/packager": "^20.0.0",
    "@types/node": "25.6.0",
    "electron": "42.0.0",
    "typescript": "5.9.3"
  }
}
